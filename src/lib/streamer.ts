/**
 * Return a Streamer
 *
 * ### Example (es imports)
 * ```js
 * import { Streamer } from 'Streamer'
 * Streamer()
 * ```
 *
 * @returns Streamer an instance
 */
export function Streamer () {
  return 42
}
